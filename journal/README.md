# SYNTEE Development Journal

Entries are named `YYYY-MM-DD--NNN-description.md`. The `--NNN` suffix preserves ordering for multiple entries on the same date. To add a new entry, use the next sequence number for today's date.
